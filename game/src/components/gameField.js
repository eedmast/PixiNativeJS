const bgTileContext = new PIXI.GraphicsContext()
    .rectangle(0, 0, 20, 20)
    .fill(0xCCCCCC);

const tiles = [];

init = function() {
    for (let i = 0; i < 15; i++){
        for(let j = 0; j < 15; j++) {
            
        }
    }
}